

public enum SceneType
{
    TitleScene = 0,
    LoadScene = 1,
    MainScene = 2,
}


public enum UnitType
{
    Defualt,
    Named,
    Boss,
    Player,
    Minion,
}

public enum Faction
{
    Enemy,
    Neutral,
    Player
}

public enum Rarity
{
    Basic,
    Common,
    Uncommon,
    Rare,
}

public enum CardRangeType
{
    Melee,
    Range,
    Act,
}

public enum XmlPath
{
    UnitInfo,
    CardInfo,
}

public enum e_WaveState
{
    Prepare,
    EnemyWave,
    BossWave,
}