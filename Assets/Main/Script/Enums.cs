

public enum SceneType
{
    TitleScene = 0,
    MainScene = 1,
}


public enum UnitType
{
    Defualt,
    Named,
    Boss,
    Player,
    Minion,
}

public enum Faction
{
    Enemy,
    Neutral,
    Player
}

public enum Rarity
{
    Common,
    Uncommon,
    Rare,
}

public enum CardType
{
    Weapon,
    Action,
    Active,
    Passive,
}